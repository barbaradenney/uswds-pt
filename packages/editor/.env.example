# ==============================================================================
# USWDS-PT Editor (Frontend) — Environment Variables
# ==============================================================================
# Copy this file to .env and fill in the values for your environment.
# All variables must be prefixed with VITE_ to be exposed to the browser.
# Lines prefixed with # are commented out and show their default values.

# ==============================================================================
# OPTIONAL: Backend Connection
# ==============================================================================

# URL of the USWDS-PT API server.
# When set, the editor connects to the API for authentication, saving, and
# loading prototypes. All API calls (auth, prototypes, teams, symbols) use
# this as the base URL.
#
# When NOT set (empty or omitted), the editor runs in DEMO MODE:
#   - No login/registration required
#   - Prototypes are not persisted to a server
#   - Teams, organizations, and collaboration features are unavailable
#   - Crash recovery via IndexedDB still works locally
#
# Default: (empty — demo mode)
VITE_API_URL=http://localhost:3001

# ==============================================================================
# OPTIONAL: AI Copilot
# ==============================================================================
# The AI Copilot adds a panel in the editor where users can describe what they
# want to build in natural language, and the AI generates GrapesJS code to
# create USWDS components. When disabled, the copilot panel is simply hidden
# and the editor works normally without it.

# API key for the AI provider (Claude or OpenAI).
# REQUIRED to enable the AI Copilot — when not set, the copilot panel is
# hidden entirely and no AI features are available.
# VITE_AI_API_KEY=

# AI provider to use.
# Values: "claude" | "openai"
# Default: claude
# VITE_AI_PROVIDER=claude

# AI model name. Uses the provider's recommended default when omitted.
# Default for claude: claude-sonnet-4-20250514
# Default for openai: gpt-4o
# VITE_AI_MODEL=claude-sonnet-4-20250514

# Optional secret to gate AI access via URL parameter.
# When set, the AI Copilot is only activated if the user visits with
# ?ai=<secret> in the URL. The activation persists for the browser session
# (via sessionStorage). Users can explicitly disable with ?ai=off.
# When NOT set, the AI Copilot is available to everyone (assuming the API
# key is configured above).
# VITE_AI_SECRET=
